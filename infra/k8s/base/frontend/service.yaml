apiVersion: v1
kind: Service
metadata:
  name: weather-frontend
  namespace: weather
spec:
  type: ClusterIP
  selector:
    app: weather-frontend
  ports:
    - port: 80
      targetPort: 8501
      protocol: TCP
