# model_interface_version
#   - モデルの本数・API の入出力スキーマ・特徴量カラム構造が変わった時にインクリメント
#   - 各 Docker イメージはビルド時点のこのファイルの値を使用する（env var による上書き不要）
#   - 特徴量パス（model_interface_version={N}）と Feast FeatureView 名にも使用する
#   - Blue/Green: 異なる interface_version を持つ 2 イメージを並行稼働させることで実現
model_interface_version: 2b
