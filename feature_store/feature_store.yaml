project: weather_forecast
registry:
  registry_type: sql
  path: postgresql+psycopg2://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${FEAST_DB_NAME}
provider: local
online_store:
  type: redis
  connection_string: "${REDIS_HOST}:${REDIS_PORT}"
entity_key_serialization_version: 3
